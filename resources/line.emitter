@param float width
@param float height
@param float max_horizontal_speed
@attribute vec2 position
@attribute vec2 velocity
@require aging

particle.position = vec2(random_range(-0.5, 0.5) * width, height);
particle.velocity = vec2(random_range(-0.5, 0.5) * max_horizontal_speed, 0.0);
